<h1>All Definitions</h1>
<div>
    <ol>
        <% @cards.each do |card| %>
            <li><%= card.back %></li>
        <% end %>
    </ol>
</div>

<div style="display: flex;">
    <%= button_to 'Go Back To Terms', cards_front_path(@group), method: :get, :class => 'btn-style-spc' %>
    <%= button_to 'Return To Set Home Page', group_path(@group), method: :get, :class => 'btn-style-spc' %>
</div>